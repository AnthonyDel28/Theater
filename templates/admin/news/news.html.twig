{% extends 'admin/base.html.twig' %}
{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/admin/news.css') }}">
{% endblock %}
{% block body %}
    <div class="col-12 col-sm-12 col-lg-10 col-xl-10">
        <h2 class="my-5 text-center">Gestion des actualités</h2>
        <p class="text-center" style="color: red;">
        </p>
        <table id="table-admin" class="table table-hover">
            <thead>
            <tr>
                <th>Image</th>
                <th>Titre</th>
                <th>Description</th>
                <th>Date de parution</th>
                <th>Date de modification</th>
                <th>Actions</th>
            </tr>
            </thead>
            <tbody>
            {% for post in news %}
                <tr>
                    <td><img src="{{ asset('assets/img/news/') ~ post.imageName }}"  class="admin-news-img"></td>
                    <td>{{ post.title }} </td>
                    <td>{{ post.content[:20] ~ '...' }}</td>
                    <td>{{ post.createdAt|date('d/m/Y h:m') }}</td>
                    <td>{{ post.updatedAt|date('d/m/Y h:m') }}</td>
                    <td>
                        <a href="{{ path('app_admin_edit_post', {id: post.id}) }}" class="btn btn-outline-light"
                           style="color: black"><i
                                    class="icofont-ui-edit"></i></a>&nbsp;
                        <a href="{{ path('app_delete_news', {'id': post.id}) }}" class="btn btn-outline-light"
                           style="color: red"><i class="icofont-ui-delete"></i></a>&nbsp;
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}