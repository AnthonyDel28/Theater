{% extends 'admin/base.html.twig' %}
{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/admin/spectacles.css') }}">
{% endblock %}
{% block body %}
    <div class="col-12 col-sm-12 col-lg-10 col-xl-10">
        <h2 class="my-5 text-center">Gestion des spectacles</h2>
        <p class="text-center" style="color: red;">
            <i class="icofont-warning-alt"></i> Attention! <i class="icofont-warning-alt"></i> <br>
            La saison 2022 - 2023 est déjà insérée et complète. Il n'est donc pas utile de modifier ces spectacles!
            <br>L'option pour supprimer ces spectacles a été désactivée, une suppression d'un spectacle pourrait corrompre les données !
        </p>
        <table id="table-admin" class="table table-hover">
            <thead>
            <tr>
                <th>Titre</th>
                <th>Auteur</th>
                <th>Date de début</th>
                <th>Date de fin</th>
                <th>Image</th>
                <th>Actions</th>
            </tr>
            </thead>
            <tbody>
            {% for post in spectacles %}
                <tr>
                    <td>{{ post.titre }}</td>
                    <td>{{ post.author }}</td>
                    <td>{{ post.startDate|date('d/m/Y') }}</td>
                    <td>{{ post.endDate|date('d/m/Y') }}</td>
                    <td><img src="{{ asset('assets/img/spectacles/') ~ post.imageName }}"  class="admin-spectacle-img"></td>
                    <td>
                        <a href="" class="btn btn-success"><i class="icofont-ui-edit"></i></a>&nbsp;
                        <a href="" class="btn btn-danger"><i class="icofont-ui-delete"></i></a>&nbsp;
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}