{% extends 'admin/base.html.twig' %}
{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/admin/spectacles.css') }}">
{% endblock %}
{% block body %}
    <div class="col-12 col-sm-12 col-lg-10 col-xl-10">
        <h2 class="my-5 text-center">Gestion des spectacles</h2>
        <p class="text-center" style="color: red;">
            <i class="icofont-warning-alt"></i> Attention! <i class="icofont-warning-alt"></i> <br>
            La saison 2022 - 2023 est déjà insérée et complète. Pour le bon fonctionnement de l'application et
            l'intégrité des données, la suppression et l'ajout de spectacles ne seront disponibles qu'à la fin de
            chaque saison.
        </p>
        <table id="table-admin" class="table table-hover">
            <thead>
            <tr>
                <th>Titre</th>
                <th>Auteur</th>
                <th>Date de début</th>
                <th>Date de fin</th>
                <th>Image</th>
                <th>Actions</th>
            </tr>
            </thead>
            <tbody>
            {% for post in spectacles %}
                <tr>
                    <td>{{ post.titre }}</td>
                    <td>{{ post.author }}</td>
                    <td>{{ post.startDate|date('d/m/Y') }}</td>
                    <td>{{ post.endDate|date('d/m/Y') }}</td>
                    <td><img src="{{ asset('assets/img/spectacles/') ~ post.imageName }}"  class="admin-spectacle-img"
                    </td>
                    <td>
                        <a href="{{ path('app_admin_edit_spectacle', { id: post.id }) }}" class="btn btn-outline-light"
                           style="color: black"><i
                                    class="icofont-ui-edit"></i></a>&nbsp;
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}