{% extends 'base.html.twig' %}
{% block body %}
    <section class="body">
        <div class="container-fluid slide">
            <div class="row slider">
                <div class="container">
                    <div class="row d-flex flex-row-reverse">
                        <div class="col-5 slide-item text-center">
                            <p class="slide-item-main-title">La Revue 2023</p>
                            <p class="slide-item-sub-title">"Mieux vaut Qatar que jamais"</p>
                            <button type="button" class="btn btn-outline-light m-2"><i
                                        class="icofont-info-circle"></i> En savoir plus</button>
                            <button type="button" class="btn btn-danger m-2"><i class="icofont-ticket"></i> Réserver
                            </button>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="container-fluid sub-slide-title">
            <div class="row slide-sub-title text-center">
                <p class="slide-sub-title-text mt-3">La Saison 2022-2023</p>
            </div>
        </div>
        <div class="container-fluid season-slide justify-content-center text-center bg-black">
            <div class="row justify-content-center text-center">
                {% set index = 1 %}
                {% for show in spectacles %}
                    <div class="season-container col-12 col-sm-8 col-md-6 col-lg-4">
                        <div class={{ "show#{index}" }}>
                            <div class="season-card-content">
                                <h1>{{ show.titre }}</h1>
                                <h3>{{ show.period }}</h3>
                            </div>
                        </div>
                    </div>
                    {% set index = index + 1 %}
                {% endfor %}
            </div>
        </div>
        <div class="container-fluid twins">
            <div class="row">
                <div class="twin1 col-xl-6 ccol-lg-6 col-sm-12 col-12">
                    <div class="twin-content text-center">
                        <h2 class="twin-text">
                            Envie de rejoindre l'équipe des Molières & Mocassins?
                        </h2>
                        <button class="btn news-btn">Nous rejoindre</button>
                    </div>
                </div>
                <div class="twin2 col-xl-6 ccol-lg-6 col-sm-12 col-12">
                    <div class="twin-content text-center">
                        <h2 class="twin-text">
                            La Revue 2023 pointe enfin le bout de son nez
                        </h2>
                        <button class="btn news-btn">Découvrir</button>
                    </div>
                </div>
            </div>
        </div>
        <div class="container-fluid news">
            <div class="row">
                <div class="container">
                    <div class="row text-center news-section">
                        <p class="news-main-title mt-3">Actualités</p>
                    </div>
                    <div class="row justify-content-evenly mb-5 home-news">
                        {% for post in news %}
                            <div class="card col-10 col-sm-8 col-md-5 col-lg-5 col-xl-2 mb-4 bg-black" style="border: none;">
                                <img class="card-img-top" src="assets/img/news/{{ post.imageName }}" alt="Card image cap">
                                <div class="card-home-news-body p-3">
                                    <h3 class="card-home-news-title"><b>{{ post.title }}</b></h3>
                                    <p class="card-home-news-text">{{ post.smallDescription }}</p>
                                    <span class="card-home-news-date">{{ post.createdAt|date('d/m/Y') }}</span>
                                </div>
                                <div class="card-home-news-body-bottom p-3">
                                    <a href="#" class="card-home-news-link"><button class="btn btn-outline-light">Lire l'article</button></a>
                                </div>
                            </div>
                        {% endfor %}
                    </div>
                </div>
            </div>
        </div>
        <div class="container-fluid quote">
            <div class="row text-center justify-content-center">
                <div class="col-10 col-lg-6">
                    <h3 class="quote-content">
                        <span class="quote-fx">"</span>Le théâtre est un indéniable outil de développement de
                        l'individu
                        . Il aide à
                        lever
                        certaines inhibitions, il optimise la créativité, il permet d'oublier le stress de la vie quotidienne,
                        il apprend à vivre ensemble puisqu'il est toujours une aventure collective.<span
                            class="quote-fx">"</span>
                    </h3>
                </div>
            </div>
        </div>
    </section>
{% endblock %}